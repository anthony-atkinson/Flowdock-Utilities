'use strict';

describe('myApp.notifier module', function() {

  beforeEach(module('myApp.notifier'));

  describe('notifier controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('NotifierCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});